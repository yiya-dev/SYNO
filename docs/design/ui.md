# UI Direction

SYNO should prioritize a fast music-making workflow rather than reproduce another DAW's interface pixel-for-pixel.

Target areas:

- transport
- timeline
- track controls
- mixer
- piano roll
- plugin windows
- browser/library
- project/version controls
- sync status

A persistent global sync indicator should make the local/remote state visible without interrupting creative work.
