# UX Direction

Important UX goals:

- autosave without making the user think about it;
- clear offline/online state;
- explicit conflict recovery;
- simple project restore;
- plugin compatibility warnings before playback is blocked;
- one-click snapshots;
- fast startup;
- keyboard-first workflow with optional touch.
